#include <bits/stdc++.h>

int main()
{
	std::ios::sync_with_stdio(0);
	std::cin.tie(0);
	
	int num;
	std::cin >> num;

	int arr[5];
	int result = 0;

	for (int i = 0; i < 5; ++i)
	{
		std::cin >> arr[i];
		if (num == arr[i])
			result++;
	}

	std::cout << result;

	return 0;
}
