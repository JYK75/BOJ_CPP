#include <iostream>
#include <vector>
#include <map>
int main()
{
    std::ios::sync_with_stdio(0);
    std::cin.tie(0);
    
    int n;
    std::map<int, int> data;
    for (int i = 0; i < 10; ++i)
    {
        std::cin >> n;
        data[n % 42]++;
    }

    std::cout << data.size();

    return 0;
};
