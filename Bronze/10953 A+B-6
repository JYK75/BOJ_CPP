#include <bits/stdc++.h>

int main()
{
	int T; 
	std::cin >> T;

	while (T--)
	{
		std::string str;
		std::cin >> str;

		int sum = 0;
		for (int i = 0; i < 3; ++i)
		{
			if (str[i] == ',')
				continue;
			
			sum += str[i] - '0';
		}
		std::cout << sum << std::endl;
	}

	return 0;
}
