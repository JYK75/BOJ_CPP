#include <iostream>

int main()
{
    std::ios::sync_with_stdio(0);
    std::cin.tie(0);

    int a, b, c;
    std::cin >> a >> b >> c;
    if (b >= c)
    {
        std::cout << - 1;
        return 0;
    }

    std::cout << a / (c - b) + 1;

    return 0;
};
