#include <bits/stdc++.h>

int main()
{
	std::ios::sync_with_stdio(0);
	std::cin.tie(0);

	int T;
	std::cin >> T;
	std::map<int, int> data;
	int num;
	for (int i = 0; i < T; ++i)
	{
		std::cin >> num;
		data[num]++;
	}
	
	for (auto d : data)
		std::cout << d.first << ' ';

	return 0;
}
