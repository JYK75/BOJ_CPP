#include <bits/stdc++.h>

int arr[5000001];
int main()
{
	std::ios::sync_with_stdio(0);
	std::cin.tie(0);

	int n, m;
	std::cin >> n >> m;

	int num;
	for (int i = 0; i < n; ++i)
	{
		std::cin >> num;
		arr[i] = num;
	}
	
	std::sort(arr, arr + n);

	std::cout << arr[m - 1];
	return 0;
}
