#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main()
{
    int N; cin >> N;
    vector<int> arr;
    
    int num;
    for(int i = 0; i < N; i++)
    {
        cin >> num;
        arr.emplace_back(num);
    }
    
    sort(arr.begin(), arr.end());
    
    for(int i = 0 ; i < arr.size(); i++)
        cout << arr[i] << '\n';
        
    return 0;
}
