#include <bits/stdc++.h>

int main()
{
	std::ios::sync_with_stdio(0);
	std::cin.tie(0);
	
	int n;
	std::cin >> n;

	long long sum = 0;
	for (int i = 1; i <= n; ++i)
	{
		sum += i * (n / i);
	}

	std::cout << sum;
	return 0;
}
