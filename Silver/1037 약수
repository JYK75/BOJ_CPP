#include <bits/stdc++.h>

int main()
{
	std::ios::sync_with_stdio(0);
	std::cin.tie(0);
	
	int n;
	std::cin >> n;
	int arr[51];
	for (int i = 0; i < n; ++i)
	{
		std::cin >> arr[i];
	}
	std::sort(arr, arr + n);
	
	std::cout << arr[0] * arr[n - 1];

	return 0;
}
